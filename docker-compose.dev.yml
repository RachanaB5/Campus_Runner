# Development docker-compose override - use with: docker-compose -f docker-compose.yml -f docker-compose.dev.yml up

services:
  backend:
    environment:
      - FLASK_DEBUG=true
      - LOG_LEVEL=DEBUG
    volumes:
      - ./backend:/app/backend:cached
      - /app/backend/__pycache__

  frontend:
    environment:
      - VITE_ENVIRONMENT=development
